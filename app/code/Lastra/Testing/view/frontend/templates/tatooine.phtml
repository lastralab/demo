<?php
$viewModel = $block->getData('view_model');
$bonuses = $viewModel->getNumberOfBonuses();
?>
<p>
  <strong>
    <?= __("Important note:") ?>
  </strong>
  <?= $bonuses > 1 ? __("You have %1 bonuses.",  $bonuses) : __("You have %1 bonus.",  $bonuses) ?>
  /p>
